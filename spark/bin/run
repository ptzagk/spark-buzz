#!/bin/bash

spark-submit \
  --master $BUZZ_MASTER \
  --driver-memory 1g --executor-memory 2g --executor-cores 1 \
  jobbuzz-spark.jar \
  --kafka-topic job-tweets \
  --kafka-brokers jobbuzz_kafka:9092
